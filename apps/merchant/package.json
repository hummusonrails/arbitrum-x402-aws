{
  "name": "@x402-aws/merchant",
  "version": "0.1.0",
  "private": true,
  "description": "x402 merchant on AWS CloudFront with Lambda@Edge and the CDP facilitator",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "cdk": "cdk",
    "synth": "cdk synth",
    "deploy": "cdk deploy --require-approval never",
    "destroy": "cdk destroy --force",
    "print-cdp-env": "ts-node scripts/print-cdp-env.ts"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "@x402-aws/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^20.14.10",
    "aws-cdk": "^2.1128.0",
    "aws-cdk-lib": "^2.256.1",
    "constructs": "^10.3.0",
    "esbuild": "^0.23.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
